@using EliteWebTechAssignment.Domain.Models
@model EliteWebTechAssignment.BusinessServices.ViewModels.AddStudentIntakeYearViewModel

<link rel="stylesheet" href="~/css/Create.css" />
<script type="text/javascript" src="~/lib/jquery/dist/jquery.min.js"></script>
<script type="text/javascript" src="~/js/Create.js"></script>

@if (Model.isSuccess != null)
{
    if ((bool)Model.isSuccess)
    {
        <div class="alert">
            The student intake year was added successfully!
            <p>You'll be automatically redirected in <span id="count">5</span> seconds...</p>
        </div>
    }
}

<form method="post" asp-action="AddStudentIntakeYear">
    <div class="border p-3">
        <div class="form-group row">
            <h2 class="text-black-50 pl-3">
                Add Intake Year
            </h2>
        </div>
        <div class="row">
            <div class="col-12">
                <div class="form-group row">
                    <div class="col-6">
                        <label asp-for="@Model.studentId"></label>
                    </div>
                    <div class="col-6">
                        <label asp-for="@Model.intakeYear"></label>
                    </div>
                </div>

                <div class="form-group row">
                    <div class="col-6">
                        <select id="StudentSelector" asp-for="@Model.studentId" class="form-control" asp-items="@(new SelectList(Model.students, nameof(StudentEntityModel.studentId), nameof(StudentEntityModel.studentName)))">
                            <option value="-1" selected>---Select Student---</option>
                        </select>
                    </div>
                    <div class="col-6">
                        <input asp-for="@Model.intakeYear" class="form-control"></input>
                    </div>
                </div>

                <div class="form-group row">
                    <div class="col-8 offset-2 row">
                        <div class="col myBtn">
                            <input type="submit" class="btn btn-info w-75" value="Create" />
                        </div>
                        <div class="col myBtn">
                            <a asp-action="Index" class="btn btn-danger w-75">Back</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>